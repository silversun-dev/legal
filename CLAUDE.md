# legal

<!-- INVENTARIO:INICIO -->
## 📋 Inventario del proyecto (OBLIGATORIO)

Este repo tiene un subinventario en `INVENTARIO.md` (raíz). Forma parte de una "doble contabilidad" con el repo maestro `silversun-dev/inventario`.

1. **Al empezar cualquier sesión**: lee `INVENTARIO.md` para conocer estado, decisiones pendientes y avisos.
2. **Si pone "SIN RELLENAR"**: antes o junto con la tarea pedida, analiza el repo y rellena todas las secciones:
   - % listo para publicar (Google Play u otra vía) con justificación breve.
   - Trabajo autónomo pendiente con tokens estimados por tarea y total.
   - Decisiones pendientes del usuario.
   - Pruebas manuales que debe hacer el usuario (compilar, instalar en móvil, plugins…).
   - Cómo probar la app: busca el enlace existente (README, `.github/workflows`, releases, GitHub Pages, `package.json` homepage, etc.). Si no hay, indícalo.
   - Peligros / problemas / avisos detectados.
   Luego cambia el estado a "✅ RELLENADO".
3. **Al terminar la sesión** (o tras cambios relevantes): actualiza `INVENTARIO.md` — tareas hechas fuera, nuevas pendientes, % recalculado, fecha, y una fila en el historial. Haz commit junto con el resto del trabajo.
4. No edites el repo `inventario` desde aquí: el maestro se sincroniza cuando el usuario pida "actualizar la situación" desde ese repo.
5. **Guardado = subido a GitHub.** Al terminar la sesión, o si el usuario dice que va a cerrar el chat, haz commit y `git push` a la **rama por defecto** del repo (fusiona tu rama en ella). Lo que solo está en local o en otra rama NO cuenta como guardado. Si no puedes hacer push, díselo claramente al usuario.
<!-- INVENTARIO:FIN -->
